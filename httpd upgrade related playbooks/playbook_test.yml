---
  - name: test playbook scripts
    hosts: "{{ specific_host }}"
    gather_facts: no
    become: yes

    tasks:
      - name: current system date
        command: "date +%Y%m%d"
        register: current_date

      - name: display current date
        debug:
          var: current_date