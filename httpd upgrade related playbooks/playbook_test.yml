---
  - name: modufy httpd.conf contents
    hosts: httpd_servers
    gather_facts: no
    become: yes

    tasks:
      - name: Modify contents of httpd.conf (Document Root)
        lineinfile:
          path: /opt/apache2.4.54_instance1/conf/httpd.conf
          regexp: '^.*apache2\.4\.48_instance1.*$'
          line: 'apache2.4.54_instance1'